{
  "name": "@blueprintui/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.15.1"
  },
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:watch": "pnpm -r build:watch",
    "clean": "git clean -dfX",
    "start": "cd docs && lite-server",
    "deploy": "firebase deploy --only hosting:blueprintui-cli"
  },
  "devDependencies": {
    "lite-server": "2.6.1"
  }
}